# پست شیرینگ (Post Sharing Feature)

## نمای کلی (Overview)
این فیچر به کاربران امکان اشتراک گذاری پست‌هایشان با کاربران خاص دیگر را می‌دهد.

## امکانات اضافه شده (Added Features)

### Backend
1. **مدل SharedPost جدید** (`models/SharedPost.js`)
   - ردیابی روابط اشتراک گذاری بین کاربران و پست‌ها
   - جلوگیری از اشتراک گذاری دوباره با یک کاربر

2. **API Endpoints جدید** (`routes/blogRoutes.js`)
   - `GET /api/users/search` - جستجوی کاربران برای اشتراک گذاری
   - `POST /api/blogs/:id/share` - اشتراک گذاری پست با کاربران مشخص
   - `GET /api/blogs/:id/shares` - دریافت لیست کاربرانی که پست با آن‌ها اشتراک گذاشته شده
   - `DELETE /api/blogs/:id/unshare` - حذف اشتراک گذاری
   - `GET /api/blogs/shared` - دریافت پست‌های اشتراک گذاشته شده با کاربر فعلی

3. **به‌روزرسانی GET /api/blogs**
   - اکنون هم پست‌های شخصی و هم پست‌های اشتراک گذاشته شده را برمی‌گرداند

### Frontend

1. **کامپوننت‌های جدید**
   - `UserSearch.js` - جستجو و انتخاب کاربران برای اشتراک گذاری
   - `ShareModal.js` - مودال اشتراک گذاری با امکان افزودن پیام

2. **Redux Actions جدید**
   - `searchUsers` - جستجوی کاربران
   - `shareBlog` - اشتراک گذاری پست
   - `fetchSharedBlogs` - دریافت پست‌های اشتراک گذاشته شده
   - `fetchBlogShares` - دریافت اشتراک‌های یک پست
   - `unshareBlogs` - حذف اشتراک گذاری

3. **به‌روزرسانی کامپوننت‌های موجود**
   - `BlogList.js` - نمایش پست‌های اشتراک گذاشته شده و دکمه اشتراک گذاری
   - `BlogShow.js` - نمایش اطلاعات اشتراک گذاری و دکمه اشتراک گذاری

## نحوه استفاده (How to Use)

1. **اشتراک گذاری پست:**
   - در لیست پست‌ها روی "Share" کلیک کنید
   - نام کاربران مورد نظر را جستجو کنید
   - کاربران را انتخاب کنید
   - پیام اختیاری اضافه کنید
   - روی "Share" کلیک کنید

2. **مشاهده پست‌های اشتراک گذاشته شده:**
   - پست‌های اشتراک گذاشته شده با برچسب آبی "Shared by [نام کاربر]" نمایش داده می‌شوند
   - پیام اشتراک گذاری (در صورت وجود) نمایش داده می‌شود

## ویژگی‌های امنیتی (Security Features)

- تنها صاحب پست می‌تواند آن را اشتراک گذاری کند
- کاربران تنها می‌توانند پست‌های اشتراک گذاشته شده با خودشان را ببینند
- جلوگیری از اشتراک گذاری دوباره با یک کاربر
- تمام endpoints نیاز به احراز هویت دارند

## تنظیمات Database
مدل SharedPost شامل ایندکس منحصر به فرد برای جلوگیری از اشتراک گذاری دوباره:
```javascript
sharedPostSchema.index({ _blog: 1, _sharedBy: 1, _sharedWith: 1 }, { unique: true });
```

## تست
برای تست کردن:
1. سرور را اجرا کنید: `npm run dev`
2. با دو کاربر مختلف وارد شوید
3. با یکی از آن‌ها پست ایجاد کنید
4. پست را با کاربر دوم اشتراک گذاری کنید
5. بررسی کنید که کاربر دوم پست اشتراک گذاشته شده را می‌بیند